network:
  version: 2
  ethernets:
    {{ device }}:
      dhcp4: no
      addresses: [{{ lan_ip }}/{{ prefix }}]
      gateway4: {{ gateway }}
      nameservers:
        search: [{{ domain }}]
        addresses: [{{ name_server }}]